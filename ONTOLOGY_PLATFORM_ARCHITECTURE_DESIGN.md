# 本体平台架构设计与前后端研发分工

## 1. 文档目标

本文档用于指导本体平台前后端研发落地，覆盖功能模块、页面边界、后端服务、核心接口、数据对象、研发分工和验收标准。设计基于当前 UI 原型，并结合以下核心诉求：

- 数据层支持结构化多源直连、自动生成数据查询接口、元数据主动探查。
- 数据层支持非结构化多源数据批量导入、解析、分块和证据溯源。
- 本体层支持本体自动构建、可视化、在线标注和审批。
- 本体模型仓库存储在 Jena 中，平台提供 Jena 查询、推理和通用技能能力。
- 广场支持平台智能体、工具、技能、本体模型和问答应用分享，也允许用户上传自己的智能体和工具。
- 问答界面作为应用测试入口，用于验证本体模型构建效果，并可将问答接口发布到广场。

## 2. 总体架构

### 2.1 逻辑架构

平台由七个核心域组成：

1. 数据接入域：结构化数据源、非结构化文件、元数据探查、解析任务、自动查询接口。
2. 本体建模域：自动建模、草稿管理、在线标注、审批发布、模型质量检查。
3. 模型仓库域：Jena 模型仓库、模型版本、命名图、查询接口、推理接口。
4. 技能服务域：数据查询技能、模型查询技能、关系推理技能、文档抽取技能。
5. 能力广场域：智能体、工具、技能、本体模型、问答应用的发布、审核、引用和统计。
6. 应用测试域：模型测试问答、证据展示、调用过程、反馈评测、发布到广场。
7. 平台管理域：用户、角色、权限、SSO、审计日志、任务监控。

### 2.2 技术架构建议

- 前端：SPA 或多页面应用均可，建议组件化拆分为数据、建模、模型、可视化、广场、应用测试、管理七个业务模块。
- 后端：模块化单体优先，内部按领域服务拆包；解析、自动建模、推理可作为异步任务服务。
- 业务关系库：建议 PostgreSQL 优先，也可使用 MySQL，保存用户、权限、数据源配置、任务、草稿、审批、广场资产、问答应用配置等平台元数据。
- Jena/Fuseki：保存已发布本体模型、实例数据、标注相关 RDF 数据和推理结果。Jena 是本体/RDF 仓库，不承担平台业务数据主库职责。
- 对象存储：建议 MinIO、S3 或兼容对象存储，保存原始文件、解析产物、导入包、工具配置包和附件。
- 检索与向量库：建议 OpenSearch/Elasticsearch 承载全文检索，Milvus、pgvector 或 Elasticsearch Vector 承载向量检索，用于非结构化问答和证据召回。
- 缓存：建议 Redis 缓存会话、任务进度、短期查询结果、登录态和限流计数。
- 任务队列：建议 RabbitMQ、Kafka、Redis Stream 或 Celery/RQ，承载元数据探查、文件解析、自动建模、模型发布、推理预计算等异步任务。
- 日志与监控：建议 Prometheus/Grafana 监控服务指标，OpenTelemetry 收集链路追踪，集中日志系统保存运行日志和错误日志。

### 2.3 核心数据流

```text
结构化数据接入
  -> 连接测试
  -> 元数据主动探查
  -> 字段语义建议
  -> 自动生成只读查询接口
  -> 数据查询技能 / 自动建模 / 应用测试

非结构化数据接入
  -> 文件或文件夹批量导入
  -> 文本、表格、OCR、章节解析
  -> 文档分块与证据位置
  -> 自动本体构建 / 问答检索 / 证据溯源

本体构建
  -> 自动生成草稿
  -> 在线标注确认
  -> 审批发布
  -> 写入 Jena 模型仓库
  -> 查询、推理、技能调用、应用测试
```

### 2.4 完整存储与中间件架构

Jena 只解决本体模型、RDF 三元组、实例关系和推理结果的存储查询问题。完整平台至少需要以下存储与中间件共同支撑：

| 类型 | 推荐组件 | 保存内容 | 说明 |
| --- | --- | --- | --- |
| 业务关系库 | PostgreSQL / MySQL | 用户、角色、权限、数据源、任务、草稿、审批、模型版本、广场资产、应用配置 | 平台主数据库，强事务、强一致 |
| Jena 模型仓库 | Apache Jena Fuseki | 本体 schema、实例三元组、标注 RDF、推理结果 | RDF 和本体推理核心仓库 |
| 对象存储 | MinIO / S3 | 原始文件、解析产物、导出文件、工具包、附件 | 文件和大对象统一存储 |
| 全文检索 | OpenSearch / Elasticsearch | 文档分块、解析文本、字段说明、资产搜索索引 | 支持关键词检索、证据召回 |
| 向量检索 | Milvus / pgvector / ES Vector | 文档向量、概念向量、问答语义召回向量 | 支持语义检索和 RAG |
| 缓存 | Redis | 登录态、任务进度、热点模型摘要、限流计数 | 提升响应速度，降低数据库压力 |
| 消息队列 | RabbitMQ / Kafka / Redis Stream | 探查任务、解析任务、建模任务、发布任务、推理任务 | 异步化和削峰填谷 |
| 审计与日志 | 业务库 + 日志系统 | 操作审计、接口调用、错误日志、运行日志 | 安全追溯和问题定位 |
| 配置与密钥 | Vault / KMS / 加密表 | 数据源密码、第三方密钥、SSO 密钥 | 敏感配置加密和轮换 |

### 2.5 前后端完整性补充

除功能页面和接口外，研发还必须覆盖以下横切能力：

- API 契约：统一响应格式、错误码、分页格式、异步任务状态格式、文件上传协议。
- 权限模型：菜单权限、按钮权限、数据源权限、模型权限、广场资产权限、应用调用权限。
- 多租户/工作空间：如果面向多团队使用，需增加 workspace_id 或 tenant_id 隔离。
- 异步任务：所有耗时操作必须任务化，包括元数据探查、文件解析、自动建模、模型发布、推理预计算、资产审核。
- 可观测性：接口耗时、任务耗时、队列堆积、Jena 查询耗时、向量检索耗时、模型问答成功率。
- 数据安全：数据源凭证加密、字段脱敏、查询接口只读、安全审计、下载权限控制。
- 前端体验：空状态、加载态、错误态、重试、任务进度、权限不可见、长列表分页、搜索筛选。
- 测试体系：单元测试、接口测试、连接器集成测试、解析回归测试、Jena 查询推理测试、端到端页面测试。

## 3. 功能模块总览

| 模块 | UI 页面 | 前端职责 | 后端职责 | 核心产物 |
| --- | --- | --- | --- | --- |
| 数据接入 | 数据连接、数据向导 | 数据源列表、连接表单、探查结果、解析任务 | 连接器、元数据探查、查询接口生成、文件解析 | 数据源、元数据、查询 API、解析分块 |
| 本体建模 | 建模、自动建模 | 建模方式选择、草稿确认、标注、审批入口 | 自动构建、草稿管理、标注、审批、校验 | 本体草稿、标注记录、审批记录 |
| 模型库 | 模型库、模型详情 | 模型卡片、版本、状态、能力入口 | 模型版本管理、发布、权限、Jena 适配 | 已发布模型、版本、权限 |
| 可视化 | 本体可视化 | 图谱、树、表格、节点详情 | 子图查询、摘要查询、布局数据 | 图谱结构、节点关系 |
| 技能 | 广场、应用测试 | 技能选择、测试配置、执行结果 | 技能注册、编排、执行、运行记录 | 数据查询技能、推理技能、抽取技能 |
| 广场 | 能力广场 | 资产列表、筛选、上传、引用 | 资产发布、审核、安装、统计 | 智能体、工具、技能、模型、应用 |
| 应用测试 | 应用测试问答 | 测试配置、问答、证据、调用过程、发布 | 问答编排、会话、反馈、分享接口 | 测试应用、会话、发布配置 |
| 管理 | SSO、用户管理 | 用户、角色、SSO 配置、审计展示 | IAM、权限、审计、令牌 | 用户、角色、审计日志 |

## 4. 数据层设计

### 4.1 结构化数据接入

#### 功能范围

- 支持 MySQL、PostgreSQL、Oracle、SQL Server、REST API 等多源直连。
- 支持连接测试、连接状态监控、连接配置加密保存。
- 接入后可主动探查库、Schema、表、字段、类型、主键、外键、索引、样例数据。
- 根据用户选择的表或视图自动生成统一只读数据查询接口。
- 自动查询接口支持分页、条件、排序、字段白名单、权限控制和审计。
- 查询接口供前端测试、数据查询技能、问答应用和自动建模调用。

#### 前端工作

- 数据源列表：展示名称、类型、连接状态、探查状态、接口数量、用途标签。
- 新建数据源向导：选择数据源类型、填写连接参数、测试连接、选择探查范围。
- 元数据探查页：展示库表树、字段列表、样例数据、字段语义建议。
- 查询接口管理页：展示接口名称、路径、参数、返回示例、启用状态、测试入口。
- 权限交互：不同角色看到不同连接、探查和接口发布按钮。

#### 后端工作

- 数据源连接器框架：统一封装连接、测试、查询和关闭。
- 连接配置加密：敏感信息入库前加密，接口返回时脱敏。
- 元数据探查服务：按数据源类型抽取 schema、字段和样例。
- 语义建议服务：根据字段名、注释、样例数据给出候选概念、属性和关系。
- 查询接口生成服务：为选中表或视图生成统一查询定义。
- 查询执行服务：执行只读查询，支持分页、条件、排序、限流和审计。
- 存储落点：数据源配置、元数据和查询接口定义入业务关系库；连接凭证入加密凭证表或密钥系统；查询审计入审计表；高频元数据摘要可缓存到 Redis。

#### 核心接口

```http
POST /api/data-sources
POST /api/data-sources/{id}/test
POST /api/data-sources/{id}/profile
GET  /api/data-sources/{id}/metadata
POST /api/data-sources/{id}/query-apis
GET  /api/query-apis
POST /api/query-apis/{id}/execute
```

#### 核心数据对象

- DataSource：数据源基本信息、类型、状态、用途、连接配置引用。
- DataSourceCredential：加密后的连接凭证。
- MetadataCatalog：库、Schema、表、字段、主外键、样例数据。
- QueryApi：自动生成的查询接口定义、参数、权限、状态。
- QueryAuditLog：查询人、查询条件、耗时、结果数量、状态。

#### 验收标准

- 至少支持两类关系型数据库接入和连接测试。
- 接入后可主动探查表和字段，并展示样例数据。
- 用户可选择表生成只读查询接口，并在页面测试查询结果。
- 查询接口可被数据查询技能复用。

### 4.2 非结构化数据批量导入与解析

#### 功能范围

- 支持单文件、文件夹、压缩包、对象存储路径导入。
- 支持 PDF、Word、Excel、CSV、TXT、Markdown、HTML、图片等格式。
- 支持文本抽取、表格抽取、OCR、标题层级识别、文档分块。
- 保存解析产物、来源位置、页码、章节、表格坐标和解析状态。
- 解析结果可用于自动建模、问答检索、证据溯源和模型校验。

#### 前端工作

- 文件上传/文件夹导入组件。
- 批量导入任务列表：进度、成功数、失败数、失败原因。
- 解析结果预览：原文、分块、表格、OCR 内容、证据位置。
- 用途选择：自动建模、应用测试、模型校验。

#### 后端工作

- 文件导入服务：支持批量上传、断点/失败重试、文件去重。
- 解析任务服务：异步执行解析并输出统一分块格式。
- 解析器适配层：按格式适配 PDF、Office、文本、图片 OCR。
- 解析结果存储：原始文件入对象存储，解析分块入业务库或检索库。
- 解析日志：记录失败原因、解析耗时和处理版本。
- 检索落点：文档分块和关键词索引入 OpenSearch/Elasticsearch；语义向量入 Milvus、pgvector 或其他向量库；分块元数据、证据位置和解析状态入业务关系库。

#### 核心接口

```http
POST /api/files/import
GET  /api/import-jobs/{jobId}
GET  /api/documents
GET  /api/documents/{id}/chunks
POST /api/documents/{id}/reparse
```

#### 验收标准

- 支持文件夹批量导入，并展示任务进度。
- 至少支持 PDF、Word、Markdown、TXT 解析。
- 解析结果能展示分块和来源位置。
- 自动建模任务可选择已解析文档作为输入。

## 5. 本体层设计

### 5.1 自动构建

#### 功能范围

- 输入结构化元数据、非结构化解析分块、用户建模要求和已有本体模型。
- 自动抽取概念、属性、关系、约束、同义词和字段映射。
- 支持基于已有模型扩展，处理同义概念、冲突关系和命名规范。
- 输出本体草稿，不直接发布。

#### 前端工作

- 建模入口页：选择“基于已有模型”或“基于数据自动建模”。
- 自动建模任务页：选择数据源、输入建模要求、选择抽取范围。
- 草稿确认页：展示候选概念、关系、属性、约束、置信度和证据来源。
- 修改交互：接受、拒绝、改名、合并、补充关系、补充属性。

#### 后端工作

- 自动构建任务服务：创建任务、执行抽取、管理状态。
- 本体草稿服务：保存草稿结构、候选项、证据和置信度。
- 模型对齐服务：与已有模型进行概念和关系对齐。
- 质量校验服务：检查孤立概念、重复概念、缺失 domain/range、关系冲突。

#### 核心接口

```http
POST /api/ontology-build-jobs
GET  /api/ontology-build-jobs/{jobId}
GET  /api/ontology-drafts/{draftId}
PATCH /api/ontology-drafts/{draftId}/items/{itemId}
POST /api/ontology-drafts/{draftId}/validate
```

### 5.2 在线标注与审批

#### 功能范围

- 支持在文本片段、表字段、表关系、文档章节上进行标注。
- 标注类型包括概念、属性、关系、约束、同义词、字段映射。
- 支持提交审批、审批通过、驳回、撤回。
- 审批通过后模型可发布到模型库，并写入 Jena 模型仓库。

#### 前端工作

- 标注工作台：左侧原文或表结构，中间候选项，右侧模型草稿。
- 标注操作：选择文本/字段，绑定概念、属性或关系。
- 审批队列：待我审批、我提交的、已完成。
- 审批详情：展示变更差异、证据、审批意见。

#### 后端工作

- 标注服务：保存标注对象、标注类型、来源位置和操作人。
- 审批服务：保存审批流转、意见、审批状态和版本差异。
- 版本服务：审批通过后生成模型版本。
- 发布服务：将发布版本转换为 RDF/OWL/TTL 并写入 Jena。

#### 核心接口

```http
POST /api/annotations
PATCH /api/annotations/{id}
POST /api/ontology-drafts/{draftId}/submit
POST /api/approvals/{approvalId}/approve
POST /api/approvals/{approvalId}/reject
POST /api/ontology-models/{modelId}/publish
```

### 5.3 本体可视化

#### 功能范围

- 支持图谱视图、树形视图、表格视图。
- 支持节点详情、关系详情、模型质量提示。
- 支持按类型、领域、状态、关系深度进行筛选。
- 支持从可视化页面进入标注、审批和应用测试。

#### 前端工作

- 图谱画布：节点、边、缩放、拖拽、筛选。
- 左侧结构树：类层级、属性列表。
- 右侧详情面板：名称、类型、属性、关系、来源、状态。
- 快捷入口：编辑结构、应用测试、发布分享。

#### 后端工作

- 图谱摘要接口：返回模型节点和边的摘要。
- 子图查询接口：按节点、深度、类型返回局部图。
- 详情接口：返回节点属性、关系、证据和状态。
- 质量接口：返回模型质量检查结果。

#### 核心接口

```http
GET /api/ontology-models/{id}/graph/summary
GET /api/ontology-models/{id}/graph/subgraph
GET /api/ontology-models/{id}/nodes/{nodeId}
GET /api/ontology-models/{id}/quality
```

## 6. Jena 模型仓库、查询与推理

### 6.1 模型仓库设计

本体模型审批发布后写入 Jena。建议按模型和版本拆分 named graph：

```text
urn:ontohub:ontology:{modelId}:{version}:schema
urn:ontohub:ontology:{modelId}:{version}:instances
urn:ontohub:ontology:{modelId}:{version}:annotations
urn:ontohub:ontology:{modelId}:{version}:inferred
```

业务库保存模型元数据、版本、权限、审批状态、Jena graph URI、发布记录；Jena 保存本体结构、实例数据、标注 RDF 和推理结果。

### 6.2 查询接口

#### 后端工作

- 提供 Jena 查询代理，统一封装 SPARQL 查询。
- 支持 SELECT、ASK、CONSTRUCT、DESCRIBE。
- 支持查询模板，用于技能和问答应用复用。
- 限制 graph 白名单、查询超时、最大返回行数和只读策略。
- 记录查询日志、执行耗时、调用来源和错误信息。

#### 平台接口

```http
POST /api/jena/query
POST /api/jena/construct
GET  /api/ontology-models/{id}/graphs
POST /api/query-templates
POST /api/query-templates/{id}/execute
```

### 6.3 推理接口

#### 后端工作

- 支持 RDFS 层级推理：subClassOf、subPropertyOf。
- 支持 OWL 基础推理：domain、range、equivalentClass、inverseOf。
- 支持少量业务规则推理。
- 推理结果可实时返回，也可异步物化到 inferred graph。
- 推理接口对问答应用、关系推理技能、模型校验开放。

#### 平台接口

```http
POST /api/jena/reason
POST /api/ontology-models/{id}/reasoning-jobs
GET  /api/reasoning-jobs/{jobId}
GET  /api/ontology-models/{id}/inferred-results
```

## 7. 通用技能设计

### 7.1 首期技能清单

| 技能 | 输入 | 执行逻辑 | 输出 | 调用方 |
| --- | --- | --- | --- | --- |
| 数据查询技能 | 数据源、查询条件、字段 | 调用自动生成的数据查询接口 | 表格、摘要、接口调用记录 | 应用测试、智能体 |
| 模型查询技能 | 模型、查询意图 | 选择或生成查询模板，访问 Jena | 三元组结果、业务摘要 | 应用测试、可视化 |
| 关系推理技能 | 起点、终点、关系类型 | 调用推理接口或规则引擎 | 关系路径、推理依据 | 应用测试 |
| 文档抽取技能 | 文档集合、字段要求 | 调用解析产物和抽取服务 | 结构化字段、证据位置 | 自动建模、问答 |

### 7.2 前端工作

- 技能列表和详情页。
- 技能测试面板：输入参数、运行按钮、结果展示。
- 应用测试中选择技能并查看执行过程。
- 广场中安装、引用和分享技能。

### 7.3 后端工作

- 技能注册服务：定义技能名称、类型、输入参数、输出结构、执行器。
- 技能执行服务：统一执行、鉴权、记录运行日志。
- 技能编排服务：支持问答应用组合多个技能。
- 技能运行记录：输入、输出、耗时、状态、错误信息。

### 7.4 核心接口

```http
POST /api/skills
GET  /api/skills
GET  /api/skills/{id}
POST /api/skills/{id}/test
POST /api/skills/{id}/execute
GET  /api/skills/{id}/runs
```

## 8. 能力广场设计

### 8.1 功能范围

- 平台内置智能体、工具、技能、本体模型可以发布到广场。
- 用户可以上传自己的智能体和工具。
- 问答应用可以发布到广场，作为可复用应用入口或可嵌入接口。
- 支持资产分类、标签、领域、评分、使用次数、发布者、审核状态。
- 支持安装、引用、下架、版本更新。

### 8.2 前端工作

- 广场列表：分类筛选、关键词搜索、标签过滤。
- 资产详情：描述、输入输出、依赖、使用说明、评分和评论。
- 上传入口：选择资产类型，填写配置，测试通过后提交审核。
- 我的发布：查看发布状态、版本、使用统计。
- 引用入口：安装技能、引用模型、使用智能体、测试问答应用。

### 8.3 后端工作

- 资产服务：管理智能体、工具、技能、本体模型、问答应用。
- 上传服务：保存用户上传配置、依赖、附件。
- 审核服务：提交审核、通过、驳回、下架。
- 安装引用服务：记录用户或团队引用关系。
- 统计服务：使用次数、评分、运行次数、失败次数。
- 工具安全：首期建议仅支持 HTTP 工具和平台内置函数，不开放任意代码执行。

### 8.4 核心接口

```http
POST /api/market/assets
GET  /api/market/assets
GET  /api/market/assets/{id}
POST /api/market/assets/{id}/submit-review
POST /api/market/assets/{id}/approve
POST /api/market/assets/{id}/install
POST /api/market/assets/{id}/unpublish
```

## 9. 应用测试问答设计

### 9.1 功能范围

- 问答界面作为应用测试入口，不只是普通聊天。
- 用户选择本体模型、技能、智能体、数据范围和推理策略。
- 回答结果展示答案、证据来源、引用文档、模型关系和调用过程。
- 用户可反馈答案是否正确，用于评估模型构建效果。
- 测试配置成熟后可发布为问答应用，并挂到广场分享。

### 9.2 前端工作

- 应用测试配置面板：选择模型、技能、智能体、数据范围。
- 会话区：问题输入、答案展示、证据引用、调用过程。
- 调试信息：展示数据查询、模型查询、推理路径、文档证据。
- 反馈按钮：正确、错误、缺少证据、模型关系错误。
- 发布入口：设置应用名称、描述、权限、是否分享到广场。

### 9.3 后端工作

- 应用配置服务：保存测试应用配置、权限和分享状态。
- 会话服务：保存会话、消息、上下文和用户反馈。
- 问答编排服务：组合模型查询、数据查询技能、文档检索、推理技能和智能体。
- 证据服务：统一返回文档片段、数据记录、模型关系和推理路径。
- 发布服务：将应用配置发布为广场资产，并生成可调用接口。

### 9.4 核心接口

```http
POST /api/apps
GET  /api/apps/{id}
PATCH /api/apps/{id}
POST /api/apps/{id}/test-chat
GET  /api/apps/{id}/sessions
POST /api/chat/sessions/{sessionId}/feedback
POST /api/apps/{id}/publish
POST /api/apps/{id}/invoke
```

## 10. 平台管理设计

### 10.1 功能范围

- 支持本地账号和企业 SSO。
- 支持用户、角色、权限管理。
- 支持审计日志、登录日志、关键操作日志。
- 支持任务监控：数据探查、文件解析、自动建模、模型发布、推理任务。

### 10.2 前端工作

- SSO 配置页。
- 用户列表、角色配置、权限分配。
- 审计日志列表和筛选。
- 任务监控列表和失败重试入口。

### 10.3 后端工作

- IAM 服务：用户、角色、权限、Token。
- SSO 服务：OIDC、SAML、LDAP/AD 对接。
- 审计服务：记录数据接入、模型发布、资产分享、问答应用调用。
- 任务服务：统一异步任务状态、日志、重试。

## 11. 前后端研发分工矩阵

| 工作项 | 前端团队 | 后端团队 | 联调重点 |
| --- | --- | --- | --- |
| 数据源接入 | 连接向导、状态展示、测试按钮 | 连接器、加密、测试连接 | 错误信息、连接状态、字段校验 |
| 元数据探查 | 表结构树、字段样例、语义建议展示 | 探查任务、字段样例、语义建议 | 探查进度、分页、字段映射 |
| 自动查询接口 | 接口列表、参数测试、结果表格 | 接口生成、执行、权限、审计 | 参数规范、返回结构、错误码 |
| 文件导入解析 | 上传、任务进度、结果预览 | 文件存储、解析任务、分块输出 | 大文件、失败重试、证据位置 |
| 自动建模 | 任务表单、草稿确认 | 抽取、对齐、草稿、质量校验 | 候选项结构、置信度、证据 |
| 在线标注 | 标注交互、候选确认、差异查看 | 标注保存、审批、版本差异 | 标注定位、并发编辑、审批状态 |
| 模型发布 | 发布按钮、模型状态 | RDF 转换、Jena 写入、版本管理 | 发布进度、失败回滚、权限 |
| 可视化 | 图谱、树、表格、节点详情 | 图谱摘要、子图查询、详情接口 | 大图加载、筛选、节点 ID |
| Jena 查询推理 | 仅展示产品化查询和推理结果 | 查询代理、推理接口、安全限制 | 查询结果格式、推理路径 |
| 技能 | 技能列表、测试表单、运行结果 | 技能注册、执行、运行日志 | 参数 schema、执行状态 |
| 广场 | 列表、详情、上传、安装 | 资产、审核、引用、统计 | 资产类型、版本、审核状态 |
| 应用测试 | 配置、问答、证据、反馈、发布 | 编排、会话、证据、发布接口 | 流式响应、证据结构、调用过程 |
| 管理 | 用户、角色、SSO、审计页面 | IAM、SSO、审计、任务服务 | 权限拦截、Token 过期、日志筛选 |

## 12. 前端工程设计

### 12.1 前端模块结构

```text
src/
  app/                    应用入口、路由、全局状态
  layouts/                主布局、侧边栏、顶部栏
  modules/
    data/                 数据源、元数据探查、查询接口、文件解析
    ontology/             建模、草稿、标注、审批
    models/               模型库、模型详情、版本
    visualization/        图谱、树、表格、节点详情
    marketplace/          广场、上传、详情、安装
    app-test/             应用测试问答、证据、反馈、发布
    admin/                用户、角色、SSO、审计
  components/             通用组件
  api/                    API Client 和类型定义
  stores/                 用户、权限、任务、全局配置
  utils/                  格式化、校验、下载、错误处理
```

### 12.2 前端通用能力

- 路由鉴权：未登录跳转、无权限隐藏菜单和按钮。
- API Client：统一请求头、Token 刷新、错误提示、重试策略。
- 任务轮询：元数据探查、文件解析、自动建模、模型发布、推理任务统一轮询组件。
- 文件上传：大文件、批量文件、文件夹导入、失败重试。
- 表格组件：分页、排序、筛选、列配置、批量操作。
- 状态组件：空状态、加载态、错误态、无权限态、任务进行中。
- 证据展示组件：文档片段、表格记录、模型关系、推理路径统一展示。
- 图谱组件：节点懒加载、子图展开、节点筛选、详情面板。
- 表单 schema：数据源配置、技能参数、工具上传、应用配置建议使用 schema 驱动。

### 12.3 前端不直接呈现的后端细节

面向普通用户的 UI 不直接呈现底层存储和查询语言细节，例如 Jena graph URI、SPARQL 语句、查询超时、命名图白名单等。需要调试时可在管理员或开发者模式中展示，普通页面统一表达为“模型查询、推理测试、调用过程、证据来源”。

## 13. 后端工程设计

### 13.1 建议后端服务拆包

```text
com.platform.iam               用户、角色、权限、SSO
com.platform.audit             审计日志
com.platform.task              异步任务、任务日志、重试
com.platform.datasource        数据源连接器、元数据探查
com.platform.queryapi          自动查询接口生成与执行
com.platform.document          文件导入、解析、分块
com.platform.ontology          本体草稿、自动构建、校验
com.platform.annotation        在线标注
com.platform.approval          审批流转
com.platform.jena              Jena 仓库、查询、推理
com.platform.skill             技能注册、执行、编排
com.platform.market            广场资产、上传、审核、安装
com.platform.app               应用测试、问答会话、发布
```

### 13.2 后端通用能力

- 统一鉴权与授权：接口级 RBAC、资源级 ACL、工作空间隔离。
- 统一任务框架：任务创建、排队、执行、进度、日志、取消、重试。
- 统一文件服务：上传、下载、预览、对象存储路径、权限校验。
- 统一审计：记录操作者、资源、动作、请求参数摘要、结果、时间。
- 统一错误码：连接失败、解析失败、权限不足、任务失败、查询超时等。
- 统一配置中心：数据源连接器、解析器、技能执行器、模型发布策略。
- 统一安全策略：凭证加密、字段脱敏、查询只读、工具调用白名单。
- 统一观测：日志、指标、链路追踪、告警。

## 14. 数据库与存储设计

### 14.1 存储职责划分

| 存储 | 是否必需 | 主要职责 | 不建议承担 |
| --- | --- | --- | --- |
| PostgreSQL/MySQL | 必需 | 平台业务元数据、用户权限、任务、审批、广场资产、应用配置 | 大文件、RDF 推理、向量检索 |
| Jena/Fuseki | 必需 | 本体 schema、实例三元组、RDF 查询、推理结果 | 用户权限、任务状态、业务配置 |
| 对象存储 | 必需 | 原始文件、解析产物、导出文件、上传包 | 结构化查询、权限主数据 |
| OpenSearch/Elasticsearch | 建议 | 全文检索、文档证据召回、广场搜索 | 强事务数据 |
| 向量库 | 建议 | 语义检索、RAG 召回、概念相似度 | 权限、审批、强一致配置 |
| Redis | 建议 | 缓存、会话、任务进度、限流 | 持久业务主数据 |
| 消息队列 | 建议 | 异步任务、削峰、事件分发 | 业务最终存储 |

### 14.2 关系型数据库表建议

| 表 | 说明 |
| --- | --- |
| users / roles / permissions | 用户、角色、权限 |
| data_sources | 数据源基本信息 |
| data_source_credentials | 加密连接凭证 |
| metadata_catalogs | 元数据探查结果 |
| query_apis | 自动查询接口定义 |
| query_audit_logs | 查询审计 |
| documents | 文件和文档记录 |
| document_chunks | 文档解析分块 |
| parse_jobs | 解析任务 |
| ontology_build_jobs | 自动建模任务 |
| ontology_drafts | 本体草稿 |
| ontology_draft_items | 草稿概念、属性、关系、约束 |
| annotations | 标注记录 |
| approvals | 审批记录 |
| ontology_models | 本体模型 |
| ontology_model_versions | 模型版本 |
| jena_graphs | Jena graph 映射 |
| skills | 技能定义 |
| skill_runs | 技能运行记录 |
| market_assets | 广场资产 |
| market_asset_versions | 广场资产版本 |
| apps | 问答应用配置 |
| chat_sessions / chat_messages | 会话和消息 |
| app_feedback | 问答反馈 |
| audit_logs | 审计日志 |

## 15. 权限设计

| 角色 | 权限 |
| --- | --- |
| 超级管理员 | 全部配置、用户、角色、审计、SSO |
| 数据管理员 | 数据源接入、元数据探查、查询接口生成、文件解析 |
| 本体建模人员 | 自动建模、草稿编辑、标注、提交审批、模型查看 |
| 审批人员 | 审批本体草稿、查看差异、发布确认 |
| 能力管理员 | 广场资产审核、下架、推荐 |
| 应用开发者 | 创建应用测试、组合技能、发布问答应用 |
| 普通用户 | 使用已授权模型、技能、问答应用 |

## 16. 非功能要求

- 安全：连接凭证加密，接口鉴权，数据查询只读，敏感字段脱敏。
- 权限：模型、数据源、技能、应用均需绑定团队或用户权限。
- 审计：数据源接入、查询接口调用、模型发布、广场发布、问答应用调用必须记录。
- 性能：图谱可视化使用摘要和子图接口，避免一次加载全量三元组。
- 稳定性：解析、建模、发布、推理全部异步任务化，支持失败重试。
- 可追溯：自动建模和问答结果必须保留证据来源。
- 可扩展：连接器、解析器、技能、工具、智能体均以插件化注册方式扩展。

## 17. 部署、运维与测试策略

### 17.1 部署建议

- 开发环境：前端开发服务、后端服务、PostgreSQL/MySQL、Jena、Redis、对象存储、OpenSearch/Elasticsearch、向量库可使用 Docker Compose 启动。
- 测试环境：与生产接近，必须包含任务队列、对象存储、Jena、检索库和向量库。
- 生产环境：建议容器化部署，支持水平扩展；解析、建模、推理等 worker 与 API 服务分离。
- 配置管理：数据库密码、SSO 密钥、模型服务密钥和第三方 API Key 不进入代码仓库，使用密钥系统或环境变量注入。
- 数据备份：业务关系库、Jena 数据集、对象存储、检索索引、向量索引分别制定备份和恢复策略。

### 17.2 测试策略

- 前端单元测试：表单校验、权限显示、任务状态组件、证据组件。
- 前端端到端测试：数据接入、自动建模、审批发布、应用测试、广场发布主流程。
- 后端单元测试：连接器、查询接口生成、解析器、草稿校验、权限判断。
- 后端集成测试：数据库连接、Jena 查询推理、对象存储、队列任务、检索库、向量库。
- 回归测试数据集：准备固定文档和数据库样例，用于验证解析结果、自动建模结果和问答证据。
- 性能测试：数据查询接口、图谱子图查询、Jena 查询、向量检索、问答编排链路。

### 17.3 API 与契约管理

- 使用 OpenAPI 维护接口契约。
- 前端基于 OpenAPI 生成 TypeScript 类型或 API Client。
- 后端接口变更需保持版本兼容，破坏性变更需要新增版本。
- 异步任务统一状态枚举：PENDING、RUNNING、SUCCESS、FAILED、CANCELLED。
- 错误码统一维护，避免前端通过错误文本判断逻辑。

## 18. MVP 研发范围

### 第一阶段：数据与模型闭环

- MySQL/PostgreSQL 接入。
- 元数据主动探查。
- 自动查询接口生成。
- PDF/Word/Markdown/TXT 文件夹导入与解析。
- 非结构化自动建模草稿。
- 草稿确认、在线标注、审批发布。
- 模型库和图谱可视化。

### 第二阶段：Jena 与技能

- 本体模型发布到 Jena。
- Jena 查询代理。
- RDFS/OWL 基础推理。
- 数据查询技能、模型查询技能、关系推理技能。
- 技能测试和运行记录。

### 第三阶段：广场与应用测试

- 应用测试问答。
- 证据、调用过程和反馈。
- 问答应用发布到广场。
- 智能体、工具、技能、本体模型上传、审核、安装。

### 暂缓范围

- 大规模知识图谱实体融合平台。
- 复杂多级审批流。
- 任意代码插件运行时。
- 完整数据治理、血缘和质量规则平台。
- 全量复杂 OWL 推理和大规模推理物化。

## 19. 交付验收清单

- 数据源接入后可探查元数据，并能生成可执行查询接口。
- 文件夹导入后可看到解析进度、分块结果和来源位置。
- 自动建模任务可生成本体草稿，草稿支持人工确认和在线标注。
- 本体草稿可提交审批，审批通过后生成模型版本。
- 已发布模型可进入模型库和可视化页面查看。
- 平台后端可将模型写入 Jena，并提供查询和推理接口。
- 数据查询技能可调用自动生成的数据查询接口。
- 应用测试可选择模型、技能和数据范围进行问答验证。
- 问答结果可展示答案、证据来源和调用过程。
- 问答应用、智能体、工具、技能和模型可发布或引用到广场。

## 20. AI 编码 Token 规模估算

如果由 AI 编码代理从当前原型和架构文档继续开发完整平台，token 消耗取决于是否已有工程脚手架、技术栈、第三方服务、测试数据和部署环境。以下估算按“输入 + 输出 + 工具调用上下文”的总 token 粗略计算，不等同于人力工时。

| 范围 | 主要内容 | 预估 token |
| --- | --- | --- |
| 原型增强与架构文档 | UI 原型、架构文档、接口草案、数据模型草案 | 10 万 - 30 万 |
| MVP 可运行版本 | 登录权限、数据接入、文件解析、自动建模草稿、标注审批、模型库、Jena 发布、基础应用测试 | 200 万 - 400 万 |
| 较完整产品版本 | 多数据源连接器、解析任务体系、Jena 查询推理、技能系统、广场、问答应用分享、审计、部署脚本和完整测试 | 800 万 - 1500 万 |
| 生产级增强版本 | 多租户、权限细粒度、稳定任务队列、监控告警、插件安全、性能优化、更多连接器和解析器、CI/CD、完整回归测试 | 1500 万 - 3000 万 |

更准确的估算需要先确定：

- 前端技术栈：React、Vue 或其他。
- 后端技术栈：Java/Spring、Python/FastAPI、Node.js 等。
- 是否已有统一登录、权限、任务队列、文件服务、对象存储。
- 是否需要内置大模型调用、向量检索、OCR、文档解析商业组件。
- Jena 部署方式、数据规模、推理复杂度。
- 是否要求生产级安全、监控、部署和自动化测试。

如果从零开发一个“可演示但链路完整”的 MVP，我会按 200 万 - 400 万 token 预留；如果要达到可给企业用户试用的完整平台，建议按 800 万 token 以上预留。
