# OntoLite 轻量本体平台前后端开发工作说明

## 1. 产品定位与架构原则

OntoLite 定位为轻量本体平台，面向业务专家、数据工程师和建模人员，核心目标是把“数据接入、本体构建、Jena 存储查询推理、技能复用、应用测试问答”打通。平台不做重型数据治理、复杂流程引擎或完整插件市场，优先保证核心链路短、交互清晰、接口稳定。

核心链路：

```text
结构化/非结构化数据接入
  -> 元数据探查 / 文件解析
  -> 自动本体构建 / 在线标注 / 审批
  -> 本体模型写入 Jena
  -> SPARQL 查询 / 推理 / 通用技能
  -> 应用测试问答 / 广场分享
```

技术边界建议：

- 前端：轻量 SPA 或多页面路由，按“数据、建模、模型、可视化、广场、测试问答、管理”拆模块。
- 后端：模块化单体优先，内部按领域服务拆包；后续可把数据接入、解析任务、推理服务拆为独立服务。
- 存储：业务库保存平台元数据，Jena Fuseki 保存本体模型和实例三元组，文件对象存储保存原始文件和解析产物。
- 异步任务：数据探查、文件解析、自动建模、Jena 导入、推理预计算全部走任务队列。
- 轻量化取舍：先做通用连接器、通用文件解析、通用 SPARQL/推理接口、通用数据查询技能；避免一开始支持过多复杂审批流和插件运行时隔离。

## 2. 总体模块划分

| 模块 | 前端页面 | 后端服务 | 核心产物 |
| --- | --- | --- | --- |
| 数据层 | 数据连接、数据向导、元数据探查 | 数据源服务、元数据服务、查询接口生成服务、文件解析服务 | 数据源、元数据、自动查询 API、解析文本/表格 |
| 本体层 | 建模、自动建模、在线标注、审批、可视化 | 本体构建服务、标注服务、审批服务、Jena 适配服务 | 本体草稿、标注结果、审批记录、OWL/RDF/TTL |
| Jena 能力层 | 模型详情、SPARQL 查询、推理配置 | Jena 查询服务、推理服务、模型发布服务 | SPARQL 接口、推理接口、命名图、版本 |
| 技能层 | 技能配置、技能测试 | 技能注册服务、技能执行服务 | 数据查询技能、SPARQL 技能、文档抽取技能 |
| 广场 | 能力广场、上传能力、详情页 | 资产分享服务、上传审核服务、依赖管理服务 | 智能体、工具、技能、本体模型、问答应用 |
| 应用测试问答 | 应用测试入口、会话、配置面板 | 问答编排服务、RAG/Agent 服务、会话服务 | 可测试应用、分享接口、会话记录 |
| 管理 | 用户、角色、SSO、审计 | IAM 服务、权限服务、审计服务 | 用户、角色、Token、操作日志 |

## 3. 数据层开发工作

### 3.1 结构化数据接入

后端能力：

1. 多源连接器
   - 首期：MySQL、PostgreSQL、Oracle、SQL Server、REST API。
   - 连接配置加密存储，支持测试连接、启停、连接池参数。
   - 保存数据源类型、连接参数、认证方式、命名空间、用途标签。

2. 元数据主动探查
   - 自动读取库、Schema、表、字段、类型、主键、外键、索引、样例数据。
   - 支持用户手动触发重新探查。
   - 生成字段语义建议，例如字段中文名、候选概念、候选属性、候选关系。

3. 自动生成数据查询接口
   - 接入结构化数据后，为选中的表或视图生成统一查询接口。
   - 支持列表查询、详情查询、条件查询、分页、排序、字段白名单。
   - 每个接口绑定权限、限流、审计和数据源版本。
   - 数据查询技能调用同一套接口，避免技能直接拼接 SQL。

前端工作：

- 数据源列表：展示连接状态、元数据状态、自动接口数量、用途。
- 新建数据源向导：选择类型、配置连接、测试连接、选择探查范围。
- 元数据探查页：表结构树、字段样例、候选概念/属性映射。
- 自动接口页：接口路径、参数、返回示例、启用状态、测试按钮。

关键接口建议：

```http
POST /api/data-sources
POST /api/data-sources/{id}/test
POST /api/data-sources/{id}/profile
GET  /api/data-sources/{id}/metadata
POST /api/data-sources/{id}/query-apis
POST /api/query/{apiKey}/execute
```

### 3.2 非结构化数据接入与解析

后端能力：

1. 批量导入
   - 支持单文件、文件夹、压缩包、对象存储路径。
   - 文件类型：PDF、Word、Excel、CSV、TXT、Markdown、HTML、图片。
   - 记录文件层级、文件哈希、解析状态、解析版本。

2. 解析流水线
   - 文本抽取、表格抽取、OCR、版面分块、标题层级识别。
   - 输出统一 Document Chunk：文本、页码、章节、表格、来源位置。
   - 解析结果可用于自动建模、检索问答、证据溯源。

3. 任务管理
   - 大文件和批量目录异步处理。
   - 支持失败重试、部分成功、解析日志、解析产物下载。

前端工作：

- 拖拽上传和文件夹导入。
- 解析任务列表和进度。
- 解析结果预览：原文、分块、表格、OCR 结果。
- 用途选择：自动建模、问答检索、模型校验。

关键接口建议：

```http
POST /api/files/import
GET  /api/parse-jobs/{jobId}
GET  /api/documents/{id}/chunks
POST /api/documents/{id}/reparse
```

## 4. 本体层开发工作

### 4.1 自动构建

自动构建输入包括结构化元数据、非结构化解析结果、用户建模要求、已有本体模型。输出为本体草稿，不直接发布。

后端能力：

- 概念抽取、属性抽取、关系抽取、约束抽取、同义词抽取。
- 结构化数据字段到概念/属性/关系的映射。
- 与已有模型对齐：同义概念合并、冲突检测、命名空间规范化。
- 输出本体草稿：Class、ObjectProperty、DatatypeProperty、Restriction、Annotation。

前端工作：

- 自动建模任务页：选择数据、输入要求、选择抽取范围。
- 草稿确认页：候选项、置信度、来源证据、接受/拒绝/修改。
- 模型质量提示：孤立概念、重复概念、缺失 domain/range。

关键接口建议：

```http
POST /api/ontology-build-jobs
GET  /api/ontology-build-jobs/{jobId}
GET  /api/ontology-drafts/{draftId}
PATCH /api/ontology-drafts/{draftId}/items/{itemId}
POST /api/ontology-drafts/{draftId}/validate
```

### 4.2 在线标注与审批

在线标注用于让业务人员在原文或表结构上确认概念、属性、关系和证据。审批用于把草稿变成可发布模型。

后端能力：

- 标注对象：文本 span、表字段、表关系、文档章节。
- 标注类型：概念、属性、关系、约束、同义词、映射。
- 审批流程轻量化：提交、审批通过、驳回、撤回。
- 审批记录保存变更差异、审批意见、操作者和时间。

前端工作：

- 标注工作台：左侧原文/表结构，中间候选抽取，右侧模型草稿。
- 审批队列：待我审批、我提交的、已完成。
- 差异查看：新增/修改/删除的概念和关系。

关键接口建议：

```http
POST /api/annotations
PATCH /api/annotations/{id}
POST /api/ontology-drafts/{draftId}/submit
POST /api/approvals/{approvalId}/approve
POST /api/approvals/{approvalId}/reject
```

### 4.3 可视化

前端重点是轻量图谱查看和局部编辑，避免一开始做复杂建模 IDE。

前端工作：

- 图谱视图：Class、Property、Instance、Relation 节点。
- 树形视图：类层级、属性列表。
- 表格视图：概念、属性、关系、约束。
- 节点详情：来源、审批状态、Jena URI、domain/range、关联技能。
- 快捷操作：进入标注、发起审批、进入应用测试。

后端能力：

- 模型图谱摘要接口，避免一次性加载全量三元组。
- 子图查询接口：按节点、关系深度、类型过滤。
- 图谱布局数据可后端计算，也可前端计算。

## 5. Jena 存储、查询与推理

### 5.1 Jena 存储设计

建议每个本体模型版本使用独立 named graph：

```text
urn:ontolite:ontology:{modelId}:{version}:schema
urn:ontolite:ontology:{modelId}:{version}:instances
urn:ontolite:ontology:{modelId}:{version}:annotations
```

平台业务库只保存模型元数据、版本、权限、审批状态、Jena graph URI；本体结构和实例数据以 RDF 形式写入 Jena。

### 5.2 查询接口

后端能力：

- SPARQL 查询执行：SELECT、ASK、CONSTRUCT、DESCRIBE。
- 查询模板：常用查询保存为模板，供技能调用。
- 查询安全：超时、最大返回行数、只读限制、命名图白名单。
- 查询解释：把自然语言问题映射为 SPARQL 时记录生成过程。

关键接口建议：

```http
POST /api/jena/query
POST /api/jena/construct
POST /api/jena/reason
GET  /api/ontology-models/{id}/graphs
POST /api/sparql-templates
```

### 5.3 推理接口

轻量版本优先支持可配置推理模式：

- RDFS 层级推理：subClassOf、subPropertyOf。
- OWL 简单推理：domain、range、equivalentClass、inverseOf。
- 规则推理：用户维护少量业务规则。

推理结果可以按需查询，也可以异步物化到 inferred graph。

## 6. 技能层

首期通用技能：

1. 数据查询技能
   - 输入：自然语言或结构化参数。
   - 执行：选择自动生成的数据查询接口。
   - 输出：表格、摘要、可追溯接口调用记录。

2. SPARQL 查询技能
   - 输入：本体模型、查询意图、变量约束。
   - 执行：生成或选择 SPARQL 模板。
   - 输出：三元组结果、路径、解释。

3. 文档抽取技能
   - 输入：文档集合、抽取字段。
   - 输出：结构化字段、证据位置。

4. 关系推理技能
   - 输入：起点概念、目标概念、关系类型。
   - 输出：关系路径和推理依据。

技能后端接口：

```http
POST /api/skills
POST /api/skills/{id}/test
POST /api/skills/{id}/execute
GET  /api/skills/{id}/runs
```

## 7. 广场

广场资产类型：

- 智能体：基于模型、技能、提示词、工具组合形成。
- 工具：外部 API、文档解析器、数据处理函数。
- 技能：平台内置或用户自定义的可执行能力。
- 本体模型：可引用的模型版本。
- 问答应用：绑定本体模型和技能的应用测试接口，可分享给他人使用。

后端能力：

- 资产发布、下架、版本、标签、评分、使用次数。
- 用户上传智能体和工具。
- 上传审核：轻量审核状态即可，避免复杂市场治理。
- 工具运行安全：首期建议只支持 HTTP 工具和平台内置函数，不开放任意代码执行。

前端工作：

- 广场列表、筛选、详情页。
- 上传入口：选择资产类型、配置参数、测试、提交审核。
- 引用入口：引用模型、安装技能、使用智能体、挂载问答接口。

关键接口建议：

```http
POST /api/market/assets
GET  /api/market/assets
GET  /api/market/assets/{id}
POST /api/market/assets/{id}/install
POST /api/market/assets/{id}/submit-review
```

## 8. 应用测试问答与分享

问答界面不是普通聊天窗口，而是应用测试入口，用于验证本体模型构建效果，并能把成熟配置发布到广场。

前端工作：

- 应用测试配置：选择本体模型、技能、智能体、数据范围、推理模式。
- 问答调试：展示答案、引用来源、SPARQL、数据查询调用、推理路径。
- 评测记录：用户反馈正确/错误，记录失败原因。
- 分享设置：生成问答应用，发布到广场。

后端能力：

- 会话管理。
- 问答编排：本体查询、数据查询技能、文档检索、推理技能组合。
- 调试信息：检索证据、SPARQL、调用链、耗时。
- 分享接口：公开或团队内访问，受权限控制。

关键接口建议：

```http
POST /api/apps
POST /api/apps/{id}/test-chat
GET  /api/apps/{id}/sessions
POST /api/apps/{id}/publish
POST /api/chat/sessions/{sessionId}/feedback
```

## 9. 前端开发任务拆分

### 9.1 基础工程

- 路由与布局：工作台、数据、建模、模型、可视化、广场、应用测试、管理。
- 统一组件：表格、卡片、状态标签、步骤条、表单、抽屉、弹窗、任务进度。
- 权限控制：菜单级、按钮级、接口级错误处理。
- API Client：统一请求、错误提示、任务轮询、下载。

### 9.2 页面任务

1. 数据层页面
   - 数据源列表
   - 新建数据源向导
   - 元数据探查页
   - 自动查询接口详情页
   - 文件解析任务页

2. 本体层页面
   - 建模入口
   - 自动建模任务
   - 草稿确认
   - 在线标注工作台
   - 审批队列

3. 模型与 Jena 页面
   - 模型库
   - 模型详情
   - 图谱可视化
   - SPARQL 查询控制台
   - 推理配置与测试

4. 广场页面
   - 广场列表
   - 资产详情
   - 上传智能体/工具/技能/模型
   - 我的发布

5. 应用测试页面
   - 测试应用配置
   - 问答调试
   - 调用链和证据面板
   - 发布到广场

6. 管理页面
   - SSO 配置
   - 用户角色
   - 审计日志

## 10. 后端开发任务拆分

### 10.1 基础服务

- 用户、角色、权限、SSO。
- 任务队列、任务状态、任务日志。
- 文件存储、下载、解析产物管理。
- 审计日志。

### 10.2 数据服务

- 数据源连接器框架。
- 结构化元数据探查。
- 自动查询 API 生成。
- 非结构化文件导入与解析。
- 数据查询技能执行适配。

### 10.3 本体服务

- 本体草稿模型。
- 自动构建任务。
- 草稿编辑和校验。
- 在线标注。
- 审批。
- RDF/OWL/TTL 序列化。

### 10.4 Jena 服务

- Jena graph 管理。
- 模型发布写入 Jena。
- SPARQL 查询接口。
- 推理接口。
- 查询模板和安全控制。

### 10.5 技能与问答服务

- 技能注册、测试、执行。
- 问答应用配置。
- 问答编排。
- 调试证据和调用链。
- 应用发布到广场。

### 10.6 广场服务

- 资产上传。
- 资产审核。
- 资产安装/引用。
- 资产版本。
- 使用统计。

## 11. MVP 范围建议

第一版建议只实现以下闭环：

1. PostgreSQL/MySQL 接入、元数据探查、自动生成只读查询接口。
2. PDF/Word/Markdown 文件夹导入、解析、分块。
3. 非结构化自动建模生成草稿。
4. 草稿人工确认和轻量审批。
5. 本体模型发布到 Jena。
6. SPARQL 查询和 RDFS/简单 OWL 推理。
7. 数据查询技能、SPARQL 查询技能、关系推理技能。
8. 应用测试问答，展示答案、证据、SPARQL/接口调用。
9. 广场分享平台内置技能、本体模型和问答应用；用户上传 HTTP 工具和智能体配置。
10. SSO、用户角色和审计。

暂缓内容：

- 复杂数据治理、血缘、质量规则平台。
- 全量 OWL 复杂推理和大规模推理物化。
- 任意代码插件运行时。
- 多级复杂审批流。
- 大规模知识图谱实体融合平台。

## 12. UI 原型修正建议

现有原型保持轻量多页面结构，但需要在信息表达上增加以下入口：

- 数据页：增加“元数据探查”和“自动查询接口”状态。
- 数据向导：增加“探查范围”和“生成查询接口”配置。
- 建模页：增加“在线标注”和“提交审批”能力。
- 自动建模页：结果区增加“证据来源、标注、审批”。
- 模型库/可视化页：增加“Jena Graph、SPARQL 查询、推理测试”入口。
- 广场页：增加“上传智能体/工具”和“分享问答应用”入口。
- 对话页：改名或强化为“应用测试”，展示模型效果验证、证据、SPARQL/查询调用链，并支持发布到广场。
